spring:
  application:
    name: auth-ms
  datasource:
    url: jdbc:postgresql://localhost:5435/book_social_network
    username: username
    password: password
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        format_sql: true
    database: postgresql
  database-platform: org.hibernate.dialect.PostgreSQLDialect


application:
  security:
    jwt:
      secret-key: F15C2727FAFB863E5B8CD4ADFF89B
    expiration: 8640000
